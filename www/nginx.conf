events {}

http {
    server {
        listen 80;
        server_name www.mary.asa.br;

        location / {
            root /usr/share/nginx/html;
            index index.html;
        }
    }
}
